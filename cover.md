# 言語処理系における I/O 入門

**プログラミング言語の「入力」と「出力」は、どこへ向かっているのか**

`puts "Hello, World!"` と書けば文字が画面に現れ、`File.read` と書けばファイルの中身が読めます。あまりにあたりまえに動くので、ふだんその裏側を意識することはありません。しかし、その一行の裏では言語処理系が OS の機能を呼び出し、OS はハードウェアを制御し、最終的に電子的な信号として外の世界へデータが流れ出していきます。

本書は、**プログラミング言語処理系（インタプリタやコンパイラのランタイム）が I/O をどう扱っているか** を、OS の仕組みからひもといていく入門書です。対象読者は、言語処理系をこれから学ぶ学部生や、自作言語に挑戦してみたい人を想定しています。

扱うテーマは大きく三つです。

- **言語処理系で求められる I/O とは何か** — `print` や `gets`、ファイル操作、標準入出力、文字エンコーディングなど、言語が提供する I/O 機能の全体像
- **OS ごとの I/O の違い** — システムコール、ファイルディスクリプタ、Unix と Windows の違い、改行コードやテキスト/バイナリモード
- **実際にどう実装するか** — バッファリング、ブロッキングと多重化（`select`/`poll`/`epoll`）、そして `io_uring` のような最新の非同期 I/O まで

コード例には、読み書きのバランスがよく、I/O の抽象化が洗練されている **Ruby** を主に使います。Ruby を知らなくても読めるよう、要所では補足を入れています。

> [!NOTE]
> 本書は生成 AI によって執筆された教材です。内容には誤りが含まれる可能性があるため、重要な箇所は必ず一次資料（各章末の参考文献）で確認してください。

それでは、`Hello, World!` の旅を、画面の奥側からたどっていきましょう。
