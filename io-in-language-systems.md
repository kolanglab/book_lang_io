# 言語処理系で求められる I/O

言語処理系を実装するということは、利用者に「I/O の道具一式」を提供するということでもあります。本章では、ほとんどのプログラミング言語が備えている I/O 機能を棚卸しし、それぞれが何のためにあり、処理系の側で何を用意しなければならないのかを見ていきます。具体例には Ruby を使いますが、ここで挙げる機能はどの言語にも共通するものです。

## 標準入出力という三つの窓口

プログラムが起動すると、OS は最初から三つの I/O の窓口を用意してくれます。これを **標準ストリーム（standard streams）** と呼びます。

- **標準入力（stdin）**：既定の入力元。多くの場合キーボード。
- **標準出力（stdout）**：既定の出力先。多くの場合画面。
- **標準エラー出力（stderr）**：エラーや診断メッセージ専用の出力先。多くの場合画面。

Ruby ではそれぞれ `$stdin`、`$stdout`、`$stderr`（あるいは定数 `STDIN` など）として最初から使えます。`puts` は `$stdout` への、`warn` は `$stderr` への出力です。

```ruby
$stdout.puts "結果: 42"     # 通常の出力
$stderr.puts "警告: 設定が見つかりません"  # 診断メッセージ
```

なぜ「画面に出す」だけなのに出力先を二つに分けるのでしょうか。理由は **リダイレクト** にあります。シェルで `ruby app.rb > result.txt` と書くと標準出力だけがファイルへ送られ、エラーメッセージは画面に残ります。出力を二系統に分けておくことで、「計算結果はファイルに、進行状況やエラーは画面に」という使い分けが自然にできるのです。この三本立ては UNIX の伝統的な設計で、その背景は [Advanced Programming in the UNIX Environment](#cite:stevens2013) に詳しく解説されています。

> [!TIP]
> 自作言語を作るときも、出力を最初から stdout と stderr に分けておくと、後でツールとして組み合わせやすくなります。エラーを stdout に混ぜてしまうと、`|`（パイプ）でつないだ次のプログラムがエラーメッセージを「データ」として誤って受け取ってしまいます。

## print の家族

最も基本的な出力は「文字列を出す」ことです。Ruby には用途の少しずつ違う出力メソッドがそろっています。

```ruby
print "改行なし"          # 文字列をそのまま出す
puts  "改行つき"          # 末尾に改行を足す
p     [1, 2, 3]           # デバッグ用：人間が読める形（inspect）で出す
printf "%05.2f\n", 3.14   # 書式を指定して整形する
```

これらは内部的にはどれも「文字列を組み立てて標準出力へ書き込む」という同じ動作に行き着きますが、利用者の意図に合わせた使い分けの語彙を提供している点が大切です。`p` は値の構造を確認したいデバッグ時に、`printf` は桁をそろえた表組みに、といった具合です。言語処理系の設計には、こうした「よく使う出力パターン」をどれだけ気持ちよく書けるようにするか、という側面もあります。

ここで一点、初心者がつまずきやすい話をしておきます。`puts` を呼んだ瞬間に文字が画面に出るとは限りません。多くの処理系は、効率のために出力をいったん **バッファ** に溜め込み、まとめて OS へ渡します。このため、プログラムが途中で異常終了するとバッファの中身が消えてしまうことがあります。この仕組みは第4章で詳しく扱います。

## 行・文字・かたまりを読む入力

入力側にも、読み取りたい単位に応じた語彙があります。

```ruby
line  = gets            # 1 行（改行まで）読む。入力が尽きると nil
chunk = $stdin.read(10) # 10 バイトだけ読む
whole = $stdin.read     # 入力の終わり（EOF）まで全部読む
$stdin.each_line do |l| # 1 行ずつ繰り返し処理する
  process(l)
end
```

ここで登場する **EOF（End Of File、ファイルの終わり）** は重要な概念です。これは特別な文字ではなく、「もう読むものがない」という状態を表します。キーボード入力では、UNIX 系なら `Ctrl-D`、Windows なら `Ctrl-Z` がこの状態を作り出します。`gets` が `nil` を返すのは、EOF に達したサインです。入力を扱うコードは、必ずこの「いつ終わるか分からないし、突然終わるかもしれない」という性質に向き合う必要があります。

> [!NOTE]
> 入力の「1 行」がどこで区切られるかは、改行コードの扱いに依存します。改行が `\n` なのか `\r\n` なのかは OS によって異なり、これが移植性の問題を引き起こします。詳しくは第5章で扱います。

## ファイル I/O とリソース管理

ファイルの読み書きは、I/O のもう一つの柱です。Ruby ではブロックつきの `File.open` がよく使われます。

```ruby
File.open("data.txt", "r") do |f|
  f.each_line { |line| puts line }
end   # ブロックを抜けると自動的にファイルが閉じられる
```

ここで本質的なのは、ファイルが **「使い終わったら閉じなければならない資源」** だということです。後の章で詳しく見ますが、開いているファイルは OS の中で「ファイルディスクリプタ」という有限の資源を消費します。閉じ忘れるとこの資源が少しずつ漏れ、長時間動くプログラムではやがて「ファイルを開けない」というエラーに至ります。

Ruby のブロックつき `File.open` は、ブロックを抜けるときに自動でファイルを閉じてくれます。これは「資源の確保と解放を一つの構文に閉じ込める」という設計で、閉じ忘れを構造的に防ぎます。言語が I/O を提供するとき、こうした資源を安全に扱う仕組みをセットで用意することがとても重要です。同じ発想は C++ の RAII や Python の `with` 文にも見られます。なお、GC のある言語では「IO オブジェクトがごみとして回収されるとき、ついでにファイルも閉じる」という保険が仕込まれていることも多いのですが、GC がいつ走るかは分からないため、これに頼ると fd を長く握りっぱなしにしてしまいます。閉じる操作はブロック構文や明示的な `close` で**決定的に**行うのが原則で、GC まかせは最後の安全網です。

## 文字コードとバイト列の境界

ここまで「文字列を読み書きする」と気軽に書いてきましたが、I/O の現場で実際に流れているのは **バイト列（byte sequence）** です。OS はバイトしか知りません。一方、私たちが扱いたいのは「あ」や「A」といった文字です。この「バイト ⇔ 文字」の変換を担うのが **文字エンコーディング（character encoding）** です。

たとえば「あ」という 1 文字は、UTF-8 というエンコーディングでは `E3 81 82` という 3 バイトで表現されます。ファイルにこの 3 バイトが書かれていても、UTF-8 だと知らなければ「あ」には戻せません。言語処理系は、I/O のたびにこの変換を（明示的または暗黙に）行っています。

```ruby
"あ".bytes        # => [227, 129, 130]  （0xE3, 0x81, 0x82）
"あ".encoding     # => #<Encoding:UTF-8>
```

Ruby は文字列に「どのエンコーディングで解釈すべきか」という情報を持たせる設計をとっており、`File.open` の際に外部エンコーディングと内部エンコーディングを指定できます。エンコーディングを誤ると、いわゆる **文字化け** が起きます。さらに、読んだバイト列がそのエンコーディングとして不正なこともあります（壊れたファイル、別エンコーディングの混入など）。このとき例外にするのか、置換文字（U+FFFD）に置き換えるのか、不正バイトを捨てるのかは、処理系が API として決めるべき仕様です（Ruby では `String#encode` の `invalid: :replace` などで方針を選べます）。I/O を提供する言語処理系にとって、「バイトと文字の境界をどこに引き、どこで変換するか」、そして「変換に失敗したらどうするか」は中心的な設計判断の一つです。

> [!WARNING]
> 「文字列の長さ」が何を意味するかはエンコーディングと密接に関わります。`"あ".size` は Ruby では 1（文字数）ですが、バイト数は `"あ".bytesize` で 3 です。ファイルへ書き込まれるのは常にバイトのほうです。文字数とバイト数を混同すると、ファイルの途中で文字を真っ二つに切ってしまう、といった不具合につながります。

## ネットワークとパイプを貫くストリームという抽象

ファイルや端末から少し離れたところにある I/O も見ておきましょう。ネットワーク通信（ソケット）や、別プログラムとつなぐパイプも、立派な I/O です。

```ruby
require "socket"
TCPSocket.open("example.com", 80) do |sock|
  sock.write "GET / HTTP/1.0\r\n\r\n"  # 送信
  puts sock.read                        # 受信
end
```

注目してほしいのは、ソケットに対しても `write` と `read` という、ファイルとまったく同じメソッドが使える点です。これは UNIX の有名な設計思想「**すべてはファイルである（everything is a file）**」の表れです。端末も、ファイルも、ネットワーク接続も、パイプも、言語からは同じ「**ストリーム（stream）**」（先頭から順にバイトが流れてくる、あるいは流れていく筒）という共通の抽象で扱えます。

この統一こそが、言語処理系にとって大きな恵みです。`read`/`write` という少数の操作さえ用意すれば、相手がファイルでもネットワークでも同じコードが動きます。第3章では、この統一を OS の側がどう実現しているか（ファイルディスクリプタ）を見ていきます。

## まとめ

本章では、言語処理系が提供すべき I/O 機能を概観しました。

- 標準ストリーム（stdin / stdout / stderr）の三本立て
- 出力（`print` 系）と入力（`gets`/`read` 系）の語彙、そして EOF という終わりの概念
- ファイル I/O と、資源を安全に閉じる仕組み
- バイトと文字を橋渡しする文字エンコーディング
- ファイル・ネットワーク・パイプを統一する「ストリーム」という抽象

これらはすべて、最終的には OS の機能に支えられています。次章では、その土台であるシステムコールとファイルディスクリプタへ降りていきます。
