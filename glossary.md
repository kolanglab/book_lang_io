# 用語集

本書に登場した主な用語を、五十音順とアルファベット順に近い形でまとめます。各章の説明とあわせて参照してください。

## I/O の基礎

**I/O（入出力）**
: Input/Output。プログラムが外の世界（端末、ファイル、ネットワークなど）とデータをやりとりすること。本書全体の主題。

**言語処理系**
: プログラミング言語のソースコードを解釈・実行、または翻訳するソフトウェアの総称。インタプリタとコンパイラを含む。

**ストリーム（stream）**
: 先頭から順にバイトが流れる「筒」として I/O を捉える抽象。ファイルも端末もネットワークも同じ見え方で扱える。

**EOF（End Of File）**
: 「もう読むものがない」という入力の終わりの状態。特定の文字ではなく状態を表す。Ruby では `gets` が `nil` を返すことで示される。

**文字エンコーディング（character encoding）**
: 「文字」と「バイト列」を相互変換する規則。例として UTF-8 では「あ」が 3 バイト（`E3 81 82`）になる。誤ると文字化けが起きる。

## OS とシステムコール

**カーネル（kernel）**
: OS の中核。ハードウェアに直接アクセスできる唯一の特権的なソフトウェア。

**ユーザ空間 / カーネル空間**
: アプリケーションが動く制限された世界（ユーザ空間）と、カーネルが動く特権的な世界（カーネル空間）。両者は保護のため分離される。

**システムコール（system call）**
: ユーザ空間のプログラムがカーネルに仕事を依頼する正式な入り口。`open`/`read`/`write`/`close` などがある。モード遷移を伴い、ふつうの関数呼び出しより重い。

**ファイルディスクリプタ（file descriptor, fd）**
: 開いている I/O 対象を指す小さな非負整数。`0`=標準入力、`1`=標準出力、`2`=標準エラー出力。有限資源なので使い終わったら `close` する。

**標準ストリーム**
: プロセス起動時に用意される stdin（標準入力）、stdout（標準出力）、stderr（標準エラー出力）の三つの窓口。

**errno**
: システムコールの失敗理由を表す番号。`ENOENT`（ファイルが無い）、`EAGAIN`（いますぐは処理できない）など。処理系はこれを例外へ翻訳する。

**部分読み書き（partial read / write）**
: `read`/`write` が要求したバイト数より少なく処理して戻ること。確実に全部を処理するにはループが必要。

## バッファリング

**バッファ（buffer）**
: データを一時的に溜める場所。システムコールの回数を減らすため、I/O はいったんバッファに溜めてまとめて処理される。

**フル / 行 / バッファなし**
: バッファリングの三モード。フル（満杯まで溜める、ファイル向き）、行（改行ごとに書き出す、端末向き）、なし（即書き出す、stderr 向き）。

**フラッシュ（flush）**
: ユーザ空間バッファに溜まったデータをカーネルへ吐き出す操作。Ruby では `IO#flush`。

**fsync**
: カーネルのページキャッシュにあるデータを物理ディスクまで確実に書き込ませるシステムコール。`flush` より強い永続性を保証するが遅い。

**ページキャッシュ**
: カーネルがディスクの内容を保持するバッファ。`flush` 後のデータがまだここに留まっていることがある。

## 並行・非同期 I/O

**ブロッキング / ノンブロッキング**
: 操作の完了を待つ（ブロッキング）か、待たずに即戻る（ノンブロッキング）か。ノンブロッキングで未準備の `read` は `EAGAIN` で即戻る。

**同期 / 非同期**
: 読み書きを自分で行う（同期）か、カーネルに任せて結果を後で受け取る（非同期）か。ブロッキング/ノンブロッキングとは別の軸。

**I/O 多重化（I/O multiplexing）**
: 一つのスレッドで多数の fd をまとめて監視し、準備ができたものだけを処理する技術。`select`/`poll`/`epoll`。

**select / poll / epoll**
: 多重化システムコール。`select`（fd 数に上限・O(N)）、`poll`（上限なし・O(N)）、`epoll`（登録制・O(イベント数)、Linux）。BSD/macOS には `kqueue` がある。

**レベルトリガ / エッジトリガ**
: `epoll` の通知方式。レベルは「読める状態が続く限り通知」、エッジは「読めるようになった変化のみ通知」。後者は読み切る規律が要る。

**C10K 問題**
: 1 台のサーバで 1 万（10K）の同時接続をどうさばくか、という課題。Dan Kegel が整理した。

**準備完了通知型 / 完了通知型**
: 「これから読める」を知らせる方式（`epoll` など）と、「読み書きが終わった」を知らせる方式（IOCP, io_uring）。

**IOCP（I/O Completion Ports）**
: Windows の完了通知型・非同期 I/O 機構。

**io_uring**
: 2019 年に Linux 5.1 で導入された非同期 I/O インタフェース。SQ（投入キュー）と CQ（完了キュー）という共有メモリ上の二つのリングで、システムコール回数とコピーを激減させる。ファイル I/O も真に非同期化できる。

## 言語処理系の実装

**GVL（Giant VM Lock、GIL）**
: 一度に Ruby コードを実行できるスレッドを 1 本に制限するロック。ただし I/O 待ちの間はロックを手放すため、スレッドは I/O 並行性に役立つ。

**Fiber（ファイバ）**
: 途中で中断し後で再開できる軽量な実行単位。OS スレッドより安価。

**Fiber スケジューラ**
: Ruby 3.0 で導入された、ブロッキング風に書いたコードを裏で自動的に非同期化する仕組み。I/O 機構（`epoll`/io_uring など）を差し替え可能にする。

## 参考文献について

本書で引用した文献は各章末の `#cite` リンクから「Bibliography」セクションへ飛べます。より深く学びたい読者は、UNIX 系の体系的な解説として [Advanced Programming in the UNIX Environment](#cite:stevens2013) と [The Linux Programming Interface](#cite:kerrisk2010)、OS 全般の入門として [Operating Systems: Three Easy Pieces](#cite:arpaci2018) から始めることを勧めます。
