# OS ごとの I/O の違い

ここまでは主に UNIX 系 OS（Linux や macOS）を念頭に話を進めてきました。しかし言語処理系は、たいてい複数の OS で動くことを求められます。そして I/O は、OS 間の違いが最も色濃く出る領域の一つです。本章では、移植性のある言語処理系を作るうえで避けて通れない、Unix と Windows の I/O の違いを中心に見ていきます。

## ファイルディスクリプタとハンドル

第3章で、Unix では開いた I/O 対象を **ファイルディスクリプタ**（小さな整数）で指す、と学びました。Windows はこの点からして異なります。Windows のネイティブ API では、I/O 対象を **ハンドル（HANDLE）** という不透明な値で指し、`ReadFile`／`WriteFile` といった独自のシステムコールで操作します。

| | Unix 系 | Windows |
|---|---------|---------|
| 対象の指し方 | ファイルディスクリプタ（整数） | ハンドル（HANDLE） |
| 開く | `open` | `CreateFile` |
| 読む / 書く | `read` / `write` | `ReadFile` / `WriteFile` |
| 閉じる | `close` | `CloseHandle` |

Windows にも、C ランタイムが提供する `_open`／`_read` といった「fd 風」の互換 API があり、内部でハンドルに変換されます。とはいえ根っこの設計が違うため、言語処理系は **OS の違いを吸収する層** を内部に持ち、上位には統一された `IO` オブジェクトだけを見せます。Ruby を使っていてプラットフォームの違いをほとんど意識せずに済むのは、処理系がこの差を引き受けているおかげです。

> [!NOTE]
> 「すべてはファイルである」という UNIX の思想は、Unix 系では端末もパイプもソケットも fd で統一的に扱えることを意味しました。Windows はこの統一度がやや弱く、たとえばソケットは `SOCKET` 型として、ファイルのハンドルとは別系統で扱われる場面があります。移植コードでは、この「統一されていなさ」がしばしば顔を出します。

## 改行コード問題

OS による I/O の違いとして、初心者が最初にぶつかるのがおそらく **改行コード** です。「行の終わり」を表すバイトが OS の文化によって異なります。

| OS 系統 | 改行の表現 | バイト列 |
|---------|-----------|---------|
| Unix 系（Linux, macOS） | LF | `0x0A`（`\n`） |
| Windows | CR + LF | `0x0D 0x0A`（`\r\n`） |
| 古い Mac OS（〜9） | CR | `0x0D`（`\r`） |

この違いは歴史的なもので、CR（Carriage Return、復帰）と LF（Line Feed、改行）はもともとタイプライタやテレタイプ端末の物理動作――「印字位置を行頭に戻す」と「紙を 1 行送る」――に由来します。Windows は両方を残し、Unix は LF 一つに簡略化しました。

実害は、たとえば Windows で作ったテキストファイルを Unix のプログラムで読むと、各行の末尾に余分な `\r` が残る、という形で現れます。

```ruby
line = "hello\r\n"
p line.chomp        # => "hello"   chomp は \r\n も \n も賢く取り除く
p line.chomp.bytes  # => [104, 101, 108, 108, 111]  \r は残っていない
```

Ruby の `chomp` が `\r\n` と `\n` の両方をうまく扱ってくれるのは、まさにこの問題への配慮です。処理系の I/O 機能は、こうした文化差を吸収する小さな気配りの積み重ねでできています。

## テキストモードとバイナリモード

改行コード問題と密接に関わるのが、**テキストモード** と **バイナリモード** の区別です。これは主に Windows で意味を持ちます。

Windows のテキストモードでは、I/O ライブラリが改行の自動変換を行います。書き込み時には `\n` を `\r\n` へ膨らませ、読み込み時には `\r\n` を `\n` へ縮める、という変換です。プログラム内では Unix 流の `\n` だけを扱えばよく、ファイル上では Windows 流の `\r\n` になる、という橋渡しをしてくれるわけです。

一方 **バイナリモード** では、こうした変換を一切行わず、バイトをそのまま読み書きします。画像や音声、暗号化データなど、1 バイトたりとも変えてはいけないデータには必ずバイナリモードを使わなければなりません。テキストモードでうっかり画像を読むと、たまたま現れた `0x0D 0x0A` の並びが勝手に書き換えられ、ファイルが壊れます。

Ruby ではモードフラグの `"b"` でバイナリモードを指定します。

```ruby
File.open("photo.jpg", "rb") do |f|   # "b" がバイナリモード
  data = f.read
  # ここで data の中身は 1 バイトも変換されていない
end
```

> [!IMPORTANT]
> Unix 系では、もともと改行が `\n` 一つなので、テキストモードとバイナリモードに（改行に関する）違いはありません。このため Unix だけで開発していると `"b"` を付け忘れがちで、Windows へ移植した途端にバイナリファイルが壊れる、という事故が起きます。**バイナリデータには常に `"b"` を付ける** ことを習慣にすると移植性が高まります。

## パスの違い

ファイルの場所を指す **パス（path）** の書き方も OS で異なります。

- **区切り文字** — Unix 系はスラッシュ `/`、Windows はバックスラッシュ `\`（ただし API は `/` も受け付けることが多い）。
- **ドライブ** — Windows には `C:` のようなドライブレターがあるが、Unix にはなく、すべてが単一の木（ルート `/`）にぶら下がる。
- **大文字小文字** — Unix の標準的なファイルシステムは大文字小文字を区別するが、Windows や標準的な macOS は区別しないことが多い。

言語処理系は、これらの差を埋めるためのパス操作ライブラリを提供します。Ruby なら `File.join` を使えば、その OS に適した区切り文字でパスを組み立ててくれます。

```ruby
# 区切り文字を手で書かず、File.join に任せる
path = File.join("data", "logs", "today.txt")
# Unix では "data/logs/today.txt"
```

「文字列を `+` でつないでパスを作る」のではなく、こうした専用 API を使うことが、移植性のあるコードへの第一歩です。

## エンコーディングと OS の既定

第2章で触れた文字エンコーディングにも、OS ごとの既定の違いがあります。現代の Linux や macOS は UTF-8 を既定とすることがほとんどですが、日本語版 Windows では歴史的に Shift_JIS 系のコードページ（CP932）が既定として使われてきました（近年は UTF-8 化が進んでいます）。

このため、同じ「テキストファイルを読む」コードでも、OS が違うと既定エンコーディングの違いから文字化けすることがあります。堅牢な処理系コードでは、エンコーディングを OS まかせにせず明示するのが安全です。

```ruby
# 既定に頼らず、エンコーディングを明示して開く
File.open("memo.txt", "r:UTF-8") do |f|
  f.read
end
```

## 違いをどう吸収するか：移植性の設計

ここまで見てきた違いを、言語処理系はどう乗り越えているのでしょうか。基本戦略は、**OS 依存の部分を薄い層に閉じ込め、その上に共通の抽象を載せる** ことです。Ruby を含む多くの処理系は、C で書かれた移植層が `read`/`write` と `ReadFile`/`WriteFile` の違い、改行やパスの違いを吸収し、利用者には `IO` クラスや `File` クラスという統一されたインタフェースだけを見せます。

このとき指針になるのが、**標準規格** の存在です。UNIX 系の I/O インタフェースは **POSIX**（Portable Operating System Interface）という規格でおおむね標準化されており、Linux も macOS もこれに準拠しているため、`open`/`read`/`write` はほぼ同じように使えます。一方 Windows は POSIX 準拠ではないため、ここに大きな段差が生まれます。処理系の移植層は、いわばこの段差に橋を架ける仕事をしているのです。POSIX に準拠した API の正確な仕様は [Advanced Programming in the UNIX Environment](#cite:stevens2013) が詳しく、Linux 固有の拡張については [The Linux Programming Interface](#cite:kerrisk2010) が参考になります。

> [!TIP]
> 自作言語を作るとき、最初から全 OS 対応を目指すと大変です。まず一つの OS（たとえば Linux）でしっかり動かし、I/O 部分を一箇所にまとめておくと、後で他 OS へ移植するときに「直す場所」が明確になります。「OS 依存を散らさない」ことが移植性の鍵です。

## まとめ

- Unix は **ファイルディスクリプタ**、Windows は **ハンドル** で I/O 対象を指すなど、根っこの設計が異なる。
- **改行コード**（LF / CR+LF）と **テキスト/バイナリモード** は、移植時の代表的な落とし穴。バイナリデータには必ず `"b"` を付ける。
- パスの区切り文字・ドライブ・大文字小文字、既定エンコーディングも OS で異なる。専用 API とエンコーディング明示で吸収する。
- 処理系は OS 依存を薄い移植層に閉じ込め、**POSIX** などの標準を足がかりに共通の抽象を提供する。

次章からは再び性能の話に戻り、「I/O の完了を待つ間、CPU をどう有効活用するか」という、現代的な I/O の中心課題に踏み込みます。
