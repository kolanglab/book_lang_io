# バッファリングとストリーム

前章の最後で、「`read`/`write` を呼ぶたびにシステムコールが走るのは高くつく」という問題に行き当たりました。本章のテーマである **バッファリング（buffering）** は、この問題への古典的かつ強力な答えです。なぜバッファが必要なのか、どんな種類があるのか、そしてそれが言語処理系の挙動にどう表れるのかを見ていきます。

## なぜバッファリングが必要か

ある言語処理系で、次のように 1 文字ずつ 100 万回出力するプログラムを考えます。

```ruby
1_000_000.times { print "x" }
```

もし `print` のたびに `write` システムコールを呼んでいたら、システムコールが 100 万回走ります。前章で見たとおりシステムコールはモード遷移を伴って重いので、これは大変な無駄です。一文字書くたびに郵便局へ歩いて行って一文字だけ投函するようなものです。

そこで処理系は、出力をいったんメモリ上の **バッファ（buffer、一時的な溜め場）** に貯めておき、ある程度たまってから（あるいは区切りのよいところで）まとめて一度の `write` で送り出します。手紙を箱に貯めておき、いっぱいになったらまとめて郵便局へ持っていくイメージです。これにより、システムコールの回数を劇的に減らせます。入力側も同様で、`read` で一度に大きめのかたまりを読んでバッファに置き、利用者の `gets` には小分けにして渡します。

> [!NOTE]
> ここで言うバッファは、言語処理系（や C ライブラリ）がユーザ空間に持つものです。実はカーネルもディスクの内容を **ページキャッシュ** という別のバッファに保持しています。「バッファ」は I/O の各層に登場するので、いまどの層の話をしているか意識すると混乱しません。本章は主にユーザ空間のバッファの話です。

## バッファリングの三つのモード

C 言語の標準ライブラリに由来する、伝統的な三つのバッファリングモードがあります。多くの言語処理系がこの分類を踏襲しています。

- **フルバッファリング（full buffering）**：バッファが満杯になるまで溜め、満杯になったら一気に書き出す。ファイルへの出力で標準的。スループット重視。
- **行バッファリング（line buffering）**：改行文字が来るたびに、その行までを書き出す。端末（画面）への出力で標準的。
- **バッファなし（unbuffered）**：溜めずに、書くたびに即座に `write` する。標準エラー出力で標準的。

ここに、第2章で「stderr は別系統」と述べた理由の一つが見えてきます。エラーメッセージは「いざという時に確実に届いてほしい」ので、バッファなし（または行単位）にしておくのです。もしエラー出力をフルバッファにしてしまうと、プログラムがクラッシュした瞬間にバッファ内のエラーメッセージごと消えてしまい、肝心の原因が分からなくなります。

> [!TIP]
> なぜ端末は行バッファで、ファイルはフルバッファなのでしょう。端末の相手は人間で、行ごとに結果が見えてほしい一方、ファイルへの大量出力では人間はリアルタイムに見ていないので効率を優先できるからです。処理系は出力先が端末かどうか（`isatty` というシステムコールで判定）を見て、自動的にモードを切り替えていることがよくあります。

## フラッシュ

バッファに溜まっているものを、満杯や改行を待たずに今すぐ書き出させる操作を **フラッシュ（flush）** と呼びます。Ruby では `IO#flush` です。

```ruby
print "計算中..."   # 改行がないので、端末でもバッファに留まることがある
$stdout.flush       # 明示的に吐き出すと、ここで画面に出る
heavy_computation
puts "完了"
```

「進捗を表示してから重い処理に入りたい」のに、`flush` を忘れると「計算中...」が処理の後まで画面に出ない、という現象がよく起きます。これはバグではなく、バッファリングの当然の帰結です。

また、Ruby の `IO#sync` を `true` にすると、その出力はバッファなしモードになり、毎回フラッシュされます。

```ruby
$stdout.sync = true   # 以降、書くたびに即座に反映される
```

プログラムが正常に終了するときは、処理系がバッファを自動でフラッシュしてから閉じてくれます。問題は **異常終了** です。`Kill` シグナルで強制終了されたり、`exit!`（後始末をしない終了）を呼んだりすると、バッファ内の未書き込みデータは失われます。

> [!WARNING]
> 「ログには出力したはずなのに、クラッシュ後にファイルを見たら最後の数行が無い」。これはほぼ確実にバッファのフラッシュ漏れです。重要なログは `sync = true` にするか、書き込みのたびに `flush` するのが安全です。

## flush と fsync の違い

ここは混同しやすい重要な区別です。`flush` はあくまで「**処理系のユーザ空間バッファ** をカーネルへ渡す」操作にすぎません。カーネルへ渡された後も、データはまだカーネルのページキャッシュにある状態で、**物理的なディスクには書かれていない** かもしれないのです。

データが本当にディスク（不揮発な記憶装置）まで届いたことを保証したいときは、`fsync` というシステムコールを使います。Ruby では `IO#fsync` です。

```ruby
File.open("important.log", "a") do |f|
  f.puts "重要な取引を記録"
  f.flush   # ユーザ空間 → カーネルへ
  f.fsync   # カーネル → 物理ディスクへ（電源断でも残る）
end
```

データの「居場所」を整理すると、次の三段階になります。

```mermaid
graph LR
    A["ユーザ空間バッファ<br/>（処理系内）"] -->|flush / write| B["カーネルの<br/>ページキャッシュ"]
    B -->|fsync| C["物理ディスク<br/>（不揮発）"]
```

データベースのように「コミットしたら絶対に消えない」ことが求められるソフトウェアは、要所で `fsync` を呼んで C の段階まで保証します。ただし `fsync` は物理ディスクの完了を待つため非常に遅く、むやみに呼ぶと性能が大きく落ちます。「どこまでの永続性が必要か」を見極めて使い分けることが大切です。この階層の詳細は [The Linux Programming Interface](#cite:kerrisk2010) や [Advanced Programming in the UNIX Environment](#cite:stevens2013) に詳しく書かれています。

## 対話とデッドロック

バッファリングは万能ではありません。特に **対話的なやりとり** では牙をむくことがあります。二つのプログラムをパイプでつなぎ、互いに応答しあう場面を考えます。

プログラム A が問いを `print`（改行なし、相手はパイプなのでフルバッファ）し、相手の答えを待つ。しかし、その問いは A のバッファに留まったまま B へ届いていない。B は問いが来ないので答えを返さない。A は答えが来ないので待ち続ける。両者が永遠に待ち合う **デッドロック（deadlock、すくみ合い）** です。

```ruby
# 別プロセスと対話する例。flush を忘れると相手に届かず固まる
require "open3"
Open3.popen3("cat") do |stdin, stdout, _stderr, _thread|
  stdin.print "hello\n"
  stdin.flush            # ← これが無いとパイプの相手に届かない
  puts stdout.gets       # => "hello"
end
```

この種の不具合は、出力先が端末（行バッファ）のときは起きず、パイプ（フルバッファ）につないだ途端に再現する、という厄介な性質を持ちます。「端末では動くのにパイプにつなぐと固まる」現象を見たら、まずフラッシュを疑ってください。

## ストリームという見え方

ここまで「バッファ付きの入出力」を扱ってきましたが、利用者からはこれが第2章で触れた **ストリーム（stream）** として見えています。ストリームとは「先頭から順にバイトが流れる筒」であり、バッファはその筒の途中に置かれた中継タンクのようなものです。利用者は `gets` や `print` で筒の端をつまむだけで、タンクの存在を意識しません。

言語処理系がうまく設計されていれば、利用者は「いつシステムコールが走るか」を気にせず、`puts` や `gets` を素朴に書けます。バッファリングはまさに、**性能のための複雑さを処理系の内側に隠し、利用者には単純なストリームだけを見せる** という、抽象化の好例なのです。

## まとめ

- システムコールは重いので、処理系は I/O をバッファに溜めてまとめて処理する。
- バッファリングには **フル / 行 / なし** の三モードがあり、出力先（端末かファイルか、stdout か stderr か）で使い分けられる。
- **flush** はユーザ空間バッファをカーネルへ渡すだけ。物理ディスクまで保証するには **fsync** が要る。
- バッファのフラッシュ漏れは、ログ欠落や対話のデッドロックといった分かりにくい不具合を生む。
- 利用者にはバッファは「ストリーム」という単純な抽象として隠される。

ここまでは主に Unix 的な世界を前提にしてきました。次章では視点を広げ、**OS によって I/O がどう違うか** を見ていきます。
